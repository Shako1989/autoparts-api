spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/autoparts
    username: autoparts
    password: autoparts
  data:
    redis:
      host: localhost
      port: 6379

app:
  meilisearch:
    host: http://localhost:7700
    master-key: devkey
  s3:
    endpoint: http://localhost:9000
    region: us-east-1
    access-key: minioadmin
    secret-key: minioadmin
    bucket: autoparts-local
    listings-bucket: autoparts-listings
    listings-public-base: http://localhost:9000/autoparts-listings
  jwt:
    secret: change-me-to-a-32-plus-char-dev-secret-string-please
    issuer: autoparts-api
    access-ttl: P1D
  mail:
    host: localhost
    port: 1025

logging:
  level:
    root: INFO
    az.autoparts: DEBUG
